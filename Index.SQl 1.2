Below is a clean, ready-to-submit **deliverable** containing SQL JOIN queries **and their outputs**.
I will use two example tables so the results are clear and reproducible.

---

# ✅ **Tables Used**

### **Customers**

| customer_id | name  |
| ----------- | ----- |
| 1           | Alice |
| 2           | Bob   |
| 3           | Carol |

### **Orders**

| order_id | customer_id | amount |
| -------- | ----------- | ------ |
| 101      | 1           | 50     |
| 102      | 1           | 80     |
| 103      | 2           | 40     |
| 104      | 4           | 20     |

---

# 📘 **DELIVERABLE: JOIN QUERIES AND THEIR OUTPUTS**

---

# 1️⃣ **INNER JOIN**

Returns only matching records from both tables.

### **Query**

```sql
SELECT c.customer_id, c.name, o.order_id, o.amount
FROM Customers c
INNER JOIN Orders o
    ON c.customer_id = o.customer_id;
```

### **Output**

| customer_id | name  | order_id | amount |
| ----------- | ----- | -------- | ------ |
| 1           | Alice | 101      | 50     |
| 1           | Alice | 102      | 80     |
| 2           | Bob   | 103      | 40     |

---

# 2️⃣ **LEFT JOIN**

Returns all records from the left table and matching ones from the right.

### **Query**

```sql
SELECT c.customer_id, c.name, o.order_id, o.amount
FROM Customers c
LEFT JOIN Orders o
    ON c.customer_id = o.customer_id;
```

### **Output**

| customer_id | name  | order_id | amount |
| ----------- | ----- | -------- | ------ |
| 1           | Alice | 101      | 50     |
| 1           | Alice | 102      | 80     |
| 2           | Bob   | 103      | 40     |
| 3           | Carol | NULL     | NULL   |

---

# 3️⃣ **RIGHT JOIN**

Returns all records from the right table and matching ones from the left.

### **Query**

```sql
SELECT c.customer_id, c.name, o.order_id, o.amount
FROM Customers c
RIGHT JOIN Orders o
    ON c.customer_id = o.customer_id;
```

### **Output**

| customer_id | name  | order_id | amount |
|-------------|----------|--------|
| 1           | Alice | 101      | 50     |
| 1           | Alice | 102      | 80     |
| 2           | Bob   | 103      | 40     |
| NULL        | NULL  | 104      | 20     |

---

# 4️⃣ **FULL OUTER JOIN**

Returns all records from both tables, matching when possible.

### **Query**

```sql
SELECT c.customer_id, c.name, o.order_id, o.amount
FROM Customers c
FULL OUTER JOIN Orders o
    ON c.customer_id = o.customer_id;
```

### **Output**

| customer_id | name  | order_id | amount |
| ----------- | ----- | -------- | ------ |
| 1           | Alice | 101      | 50     |
| 1           | Alice | 102      | 80     |
| 2           | Bob   | 103      | 40     |
| 3           | Carol | NULL     | NULL   |
| NULL        | NULL  | 104      | 20     |

---

# ✅ **COMPLETE & READY TO SUBMIT**

If you want, I can also:

✔ Generate a PDF-style formatted version
✔ Add diagrams for each join
✔ Use *your actual database tables* instead of examples

Would you like that?
